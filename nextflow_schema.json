{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$id": "https://raw.githubusercontent.com/bahlolab/nf-cavalier/master/nextflow_schema.json",
    "title": "nf-cavalier parameters",
    "description": "Nextflow pipeline for singleton and family based candidate variant reporting based on gene lists.",
    "type": "object",
    "definitions": {
        "input_output_options": {
            "title": "Input/output options",
            "type": "object",
            "fa_icon": "fas fa-terminal",
            "description": "Define where the pipeline should find input data and save output data.",
            "properties": {
                "outdir": {
                    "type": "string",
                    "format": "directory-path",
                    "description": "The output directory where the results will be saved.",
                    "fa_icon": "fas fa-folder-open",
                    "default": "output"
                },
                "alignments": {
                    "type": "string",
                    "fa_icon": "fas fa-align-left",
                    "description": "TSV file with alignment file paths (Col 1: sample ID, Col 2: BAM or CRAM path).",
                    "format": "file-path"
                },
                "lists": {
                    "type": "string",
                    "fa_icon": "fas fa-list-ul",
                    "description": "Gene lists, comma separated (TSV or ID). Supports local TSV files, PanelApp (PAA:/PAE:), HPO (HP:), Genes4Epilepsy (G4E:), HGNC, or genomic regions.",
                    "format": "file-path"
                },
                "ped": {
                    "type": "string",
                    "fa_icon": "fas fa-users",
                    "description": "Pedigree file. Required for familial analysis; leave null for singletons.",
                    "format": "file-path"
                },
                "short_vcf": {
                    "type": "string",
                    "fa_icon": "fas fa-database",
                    "description": "Input VCF for short variants (SNVs/Indels). Accepts bgzipped VCF (.vcf.gz) or BCF (.bcf), with a .tbi or .csi index alongside.",
                    "format": "file-path"
                },
                "struc_vcf": {
                    "type": "string",
                    "fa_icon": "fas fa-database",
                    "description": "Input VCF for structural variants. Accepts bgzipped VCF (.vcf.gz) or BCF (.bcf), with a .tbi or .csi index alongside.",
                    "format": "file-path"
                },
                "short_vcf_annotated": {
                    "type": "string",
                    "fa_icon": "fas fa-database",
                    "description": "Pre-annotated short variant VCF (skips annotation step). Accepts bgzipped VCF (.vcf.gz) or BCF (.bcf), with a .tbi or .csi index alongside.",
                    "format": "file-path"
                },
                "struc_vcf_annotated": {
                    "type": "string",
                    "fa_icon": "fas fa-database",
                    "description": "Pre-annotated structural variant VCF (skips annotation step). Accepts bgzipped VCF (.vcf.gz) or BCF (.bcf), with a .tbi or .csi index alongside.",
                    "format": "file-path"
                }
            }
        },
        "miscellaneous": {
            "title": "Miscellaneous",
            "type": "object",
            "description": "Additional pipeline options.",
            "properties": {
                "annotate_only": {
                    "type": "boolean",
                    "default": false,
                    "description": "Only perform annotation; skip filtering and reporting."
                },
                "make_slides": {
                    "type": "boolean",
                    "default": true,
                    "description": "Output PPT/PDF slides."
                },
                "qc_somalier": {
                    "type": "boolean",
                    "default": true,
                    "description": "Run somalier QC (relatedness, ancestry, contamination)."
                },
                "qc_sce_vcf": {
                    "type": "boolean",
                    "default": true,
                    "description": "Run sample-contamination-estimate (SCE) QC on the input short variant VCF."
                },
                "qc_sce_chr": {
                    "type": "string",
                    "default": "chr1,chr2,chr3",
                    "description": "Comma-separated chromosomes to use for SCE QC."
                },
                "ref_fasta": {
                    "type": "string",
                    "description": "Path to GRCh38 reference FASTA file.",
                    "format": "file-path"
                },
                "cavalier_cache_dir": {
                    "type": "string",
                    "default": "cavalier_cache",
                    "description": "Directory for the cavalier R package cache."
                },
                "cavalier_options": {
                    "type": "object",
                    "description": "Additional options passed to the cavalier R package (e.g. read_only_cache_dir).",
                    "hidden": true
                },
                "somalier_dir": {
                    "type": "string",
                    "description": "Path to existing somalier reference data.",
                    "format": "directory-path"
                },
                "igv_ideogram": {
                    "type": "string",
                    "description": "Chromosome ideogram file in UCSC cytoBandIdeo format (.txt or .txt.gz). When set alongside ref_gene, igv-reports uses local files and avoids runtime downloads from UCSC during parallel runs.",
                    "format": "file-path",
                    "default": null
                }
            }
        },
        "vep_options": {
            "title": "VEP options",
            "type": "object",
            "description": "Variant Effect Predictor annotation settings and plugin files.",
            "properties": {
                "vep_cache": {
                    "type": "string",
                    "description": "Path to VEP cache directory. Required.",
                    "format": "directory-path",
                    "default": null
                },
                "vep_cache_ver": {
                    "type": "string",
                    "default": "115",
                    "description": "VEP cache version to use."
                },
                "vep_check": {
                    "type": "boolean",
                    "default": true,
                    "description": "Check that the number of variants output by VEP equals the number input."
                },
                "vep_spliceai_snv": {
                    "type": "string",
                    "description": "SpliceAI SNV VCF path (download from Illumina BaseSpace). Set to null to disable the plugin.",
                    "format": "file-path",
                    "default": null
                },
                "vep_spliceai_indel": {
                    "type": "string",
                    "description": "SpliceAI indel VCF path (download from Illumina BaseSpace). Set to null to disable the plugin.",
                    "format": "file-path",
                    "default": null
                },
                "vep_alphamissense": {
                    "type": "string",
                    "description": "AlphaMissense annotation TSV file. Set to null to disable the plugin.",
                    "format": "file-path",
                    "default": null
                },
                "vep_revel": {
                    "type": "string",
                    "description": "REVEL annotation TSV file. Set to null to disable the plugin.",
                    "format": "file-path",
                    "default": null
                },
                "vep_utr_annotator": {
                    "type": "string",
                    "description": "UTRannotator file. Set to null to disable the plugin.",
                    "format": "file-path",
                    "default": null
                }
            }
        },
        "vcfanno_options": {
            "title": "vcfanno options",
            "type": "object",
            "description": "vcfanno annotation settings for short variant population databases.",
            "properties": {
                "vcfanno_binary": {
                    "type": "string",
                    "default": "https://github.com/brentp/vcfanno/releases/download/v0.3.7/vcfanno_linux64",
                    "description": "Path or URL for vcfanno binary.",
                    "hidden": true
                },
                "vcfanno_gnomad": {
                    "type": "string",
                    "description": "gnomAD 4.1 callset vcf.gz with INFO: AC, AF, fafmax_faf95_max, nhomalt.",
                    "format": "file-path"
                },
                "vcfanno_cadd_snv": {
                    "type": "string",
                    "description": "CADD 1.7 SNV TSV file.",
                    "format": "file-path"
                },
                "vcfanno_cadd_indel": {
                    "type": "string",
                    "description": "CADD 1.7 indel TSV file.",
                    "format": "file-path"
                },
                "vcfanno_clinvar": {
                    "type": "string",
                    "description": "ClinVar VCF with INFO: CLNSIG, GENEINFO and ID.",
                    "format": "file-path"
                },
                "vcfanno_custom": {
                    "type": "string",
                    "description": "Custom vcfanno TOML configuration file.",
                    "format": "file-path"
                }
            }
        },
        "short_variant_options": {
            "title": "Short variant options",
            "type": "object",
            "description": "Processing options for short variants (SNVs/Indels).",
            "properties": {
                "short_n_shards": {
                    "type": "integer",
                    "default": 200,
                    "description": "Split input VCF into this many shards for parallel processing."
                },
                "max_short_per_deck": {
                    "type": "integer",
                    "default": 500,
                    "description": "Maximum number of short variants per slide deck."
                },
                "short_vcf_filter": {
                    "type": "string",
                    "default": "PASS,.",
                    "description": "Pre-annotation FILTER values to retain from the input short variant VCF (comma-separated; applied before vcfanno/VEP)."
                },
                "short_info": {
                    "type": "array",
                    "default": ["AC", "AF", "AN"],
                    "description": "INFO fields to keep from the input short variant VCF. Other INFO fields are discarded before annotation."
                },
                "short_format": {
                    "type": "array",
                    "default": ["GT", "GQ", "DP"],
                    "description": "FORMAT fields to keep from the input short variant VCF. GT must be present."
                },
                "short_fill_tags": {
                    "type": "boolean",
                    "default": false,
                    "description": "Fill AC, AF, and AN tags using bcftools +fill-tags. Recommended if VCF does not have these set."
                },
                "short_vcfanno_filter": {
                    "type": "string",
                    "default": "gnomad_AF<0.01 || gnomad_AF=\".\"",
                    "description": "bcftools-style expression applied after vcfanno annotation, before VEP. Used to drop common variants and reduce VEP runtime."
                }
            }
        },
        "filtering_short": {
            "title": "Short variant filtering",
            "type": "object",
            "description": "Filtering thresholds for short variants.",
            "properties": {
                "FILTER_SHORT_MIN_DP": {
                    "type": "integer",
                    "default": 5,
                    "description": "Minimum read depth for short variants."
                },
                "FILTER_SHORT_MIN_GQ": {
                    "type": "integer",
                    "default": 10,
                    "description": "Minimum genotype quality for short variants."
                },
                "FILTER_SHORT_POP_DOM_MAX_AF": {
                    "type": "number",
                    "default": 0.0001,
                    "description": "Maximum population (gnomAD) AF for dominant short variants."
                },
                "FILTER_SHORT_POP_REC_MAX_AF": {
                    "type": "number",
                    "default": 0.01,
                    "description": "Maximum population (gnomAD) AF for recessive short variants."
                },
                "FILTER_SHORT_POP_DOM_MAX_AC": {
                    "type": "integer",
                    "default": 10,
                    "description": "Maximum population (gnomAD) AC for dominant short variants."
                },
                "FILTER_SHORT_POP_REC_MAX_AC": {
                    "type": "integer",
                    "default": 1000,
                    "description": "Maximum population (gnomAD) AC for recessive short variants."
                },
                "FILTER_SHORT_POP_DOM_MAX_HOM": {
                    "type": "integer",
                    "default": 1,
                    "description": "Maximum population (gnomAD) homozygote count for dominant short variants."
                },
                "FILTER_SHORT_POP_REC_MAX_HOM": {
                    "type": "integer",
                    "default": 10,
                    "description": "Maximum population (gnomAD) homozygote count for recessive short variants."
                },
                "FILTER_SHORT_COH_DOM_MAX_AF": {
                    "type": "number",
                    "description": "Maximum cohort AF for dominant short variants. Set to null to disable.",
                    "default": null
                },
                "FILTER_SHORT_COH_REC_MAX_AF": {
                    "type": "number",
                    "description": "Maximum cohort AF for recessive short variants. Set to null to disable.",
                    "default": null
                },
                "FILTER_SHORT_COH_DOM_MAX_AC": {
                    "type": "integer",
                    "description": "Maximum cohort AC for dominant short variants. Set to null to disable.",
                    "default": null
                },
                "FILTER_SHORT_COH_REC_MAX_AC": {
                    "type": "integer",
                    "description": "Maximum cohort AC for recessive short variants. Set to null to disable.",
                    "default": null
                },
                "FILTER_SHORT_CLINVAR_LIST_ONLY": {
                    "type": "boolean",
                    "default": true,
                    "description": "Restrict ClinVar variant reporting to genes in the gene list only."
                },
                "FILTER_SHORT_CLINVAR_KEEP_PAT": {
                    "type": "string",
                    "default": "(p|P)athogenic(?!ity)",
                    "description": "Regex pattern for ClinVar significance values to keep."
                },
                "FILTER_SHORT_CLINVAR_DISC_PAT": {
                    "type": "string",
                    "default": "(b|B)enign",
                    "description": "Regex pattern for ClinVar significance values to discard."
                },
                "FILTER_SHORT_LOF": {
                    "type": "boolean",
                    "default": true,
                    "description": "Enable TYPE='LOF' (VEP IMPACT == 'HIGH') variant class."
                },
                "FILTER_SHORT_MISSENSE": {
                    "type": "boolean",
                    "default": true,
                    "description": "Enable TYPE='MISSENSE' (VEP CSQ contains 'missense') variant class."
                },
                "FILTER_SHORT_SPLICING": {
                    "type": "boolean",
                    "default": true,
                    "description": "Enable TYPE='SPLICING' variant class."
                },
                "FILTER_SHORT_PROMOTER": {
                    "type": "boolean",
                    "default": false,
                    "description": "Enable TYPE='PROMOTER' variant class."
                },
                "FILTER_SHORT_OTHER": {
                    "type": "boolean",
                    "default": true,
                    "description": "Enable TYPE='OTHER' variant class."
                },
                "FILTER_SHORT_MIN_CADD_PP": {
                    "type": "number",
                    "default": 25.3,
                    "description": "Minimum CADD Phred score to force inclusion (ClinGen PP3 supporting threshold)."
                },
                "FILTER_SHORT_MIN_SPLICEAI_PP": {
                    "type": "number",
                    "default": 0.20,
                    "description": "Minimum SpliceAI score to force inclusion (ClinGen PP3 supporting threshold)."
                },
                "FILTER_SHORT_VEP_MIN_IMPACT": {
                    "type": "string",
                    "default": "MODERATE",
                    "description": "Retain variants with at least this VEP IMPACT as TYPE='OTHER' (requires FILTER_SHORT_OTHER=true). One of: MODIFIER, LOW, MODERATE, HIGH.",
                    "enum": ["MODIFIER", "LOW", "MODERATE", "HIGH"]
                },
                "FILTER_SHORT_VEP_CONSEQUENCES": {
                    "type": "string",
                    "description": "Additionally retain variants matching any of these VEP Consequence terms as TYPE='OTHER' (comma-separated). Complements FILTER_SHORT_VEP_MIN_IMPACT. Set to null to disable.",
                    "default": null
                },
                "FILTER_SHORT_MAX_PROMOTERAI": {
                    "type": "number",
                    "default": -0.50,
                    "description": "Maximum PromoterAI score for TYPE='PROMOTER' variants (decreased expression direction; requires FILTER_SHORT_PROMOTER=true). Thresholds: -0.5 high precision, -0.2 balanced, -0.1 high recall."
                }
            }
        },
        "struc_variant_options": {
            "title": "Structural variant options",
            "type": "object",
            "description": "Processing options and reference data for structural variants.",
            "properties": {
                "struc_vcf_filter": {
                    "type": "string",
                    "default": "PASS,.",
                    "description": "Pre-annotation FILTER values to retain from the input structural variant VCF (comma-separated)."
                },
                "struc_info": {
                    "type": "array",
                    "default": ["AC", "AF", "AN", "SVTYPE", "SVLEN", "END"],
                    "description": "INFO fields to keep from the structural variant VCF. Other INFO fields are discarded before annotation."
                },
                "struc_format": {
                    "type": "array",
                    "default": ["GT"],
                    "description": "FORMAT fields to keep from the structural variant VCF. GT must be present."
                },
                "struc_fill_tags": {
                    "type": "boolean",
                    "default": false,
                    "description": "Fill AC, AF, and AN tags for structural variants using bcftools +fill-tags."
                },
                "struc_n_shards": {
                    "type": "integer",
                    "default": 20,
                    "description": "Number of shards for parallel structural variant processing."
                },
                "max_struc_per_deck": {
                    "type": "integer",
                    "default": 500,
                    "description": "Maximum number of structural variants per slide deck."
                },
                "svafdb": {
                    "type": "string",
                    "description": "SVAFotate database path for gnomAD v4.1 SV population frequencies. Set to null to disable SV population annotation.",
                    "format": "file-path",
                    "default": null
                },
                "ref_gene": {
                    "type": "string",
                    "description": "NCBI RefSeq Select TSV file (from UCSC Table Browser). Set to null to disable RefSeq gene annotation.",
                    "format": "file-path",
                    "default": null
                }
            }
        },
        "filtering_struc": {
            "title": "Structural variant filtering",
            "type": "object",
            "description": "Filtering thresholds for structural variants.",
            "properties": {
                "FILTER_STRUC_POP_DOM_MAX_AF": {
                    "type": "number",
                    "default": 0.0001,
                    "description": "Maximum population (gnomAD) AF for dominant SVs."
                },
                "FILTER_STRUC_POP_REC_MAX_AF": {
                    "type": "number",
                    "default": 0.01,
                    "description": "Maximum population (gnomAD) AF for recessive SVs."
                },
                "FILTER_STRUC_POP_DOM_MAX_HOM": {
                    "type": "integer",
                    "description": "Maximum population (gnomAD) homozygote count for dominant SVs. Set to null to disable.",
                    "default": null
                },
                "FILTER_STRUC_POP_REC_MAX_HOM": {
                    "type": "integer",
                    "description": "Maximum population (gnomAD) homozygote count for recessive SVs. Set to null to disable.",
                    "default": null
                },
                "FILTER_STRUC_COH_DOM_MAX_AF": {
                    "type": "number",
                    "default": 0.01,
                    "description": "Maximum cohort AF for dominant SVs."
                },
                "FILTER_STRUC_COH_REC_MAX_AF": {
                    "type": "number",
                    "default": 0.01,
                    "description": "Maximum cohort AF for recessive SVs."
                },
                "FILTER_STRUC_COH_DOM_MAX_AC": {
                    "type": "integer",
                    "description": "Maximum cohort AC for dominant SVs. Set to null to disable.",
                    "default": null
                },
                "FILTER_STRUC_COH_REC_MAX_AC": {
                    "type": "integer",
                    "description": "Maximum cohort AC for recessive SVs. Set to null to disable.",
                    "default": null
                },
                "FILTER_STRUC_SVTYPES": {
                    "type": "string",
                    "default": "DEL,DUP,INS,INV",
                    "description": "Comma-separated list of SV types to retain. Note: BND is currently excluded upstream due to a VEP issue."
                },
                "FILTER_STRUC_VEP_MIN_IMPACT": {
                    "type": "string",
                    "default": "LOW",
                    "description": "Minimum VEP impact for SVs. One of: MODIFIER, LOW, MODERATE, HIGH.",
                    "enum": ["MODIFIER", "LOW", "MODERATE", "HIGH"]
                },
                "FILTER_STRUC_VEP_CONSEQUENCES": {
                    "type": "string",
                    "default": "coding_sequence_variant,non_coding_transcript_exon_variant,TFBS_ablation,regulatory_region_ablation",
                    "description": "Comma-separated VEP consequences to retain for SVs."
                },
                "FILTER_STRUC_LARGE_LENGTH": {
                    "type": "integer",
                    "description": "Automatically report SVs larger than this length (bp). Set to null to disable.",
                    "default": null
                }
            }
        },
        "reporting": {
            "title": "Reporting",
            "type": "object",
            "description": "Options controlling slide output content.",
            "properties": {
                "SLIDE_INFO_SHORT": {
                    "type": "object",
                    "description": "Fields to include in short variant slides. Must contain a DEFAULT key (applied to all variants); additional keys (MISSENSE, SPLICING, PROMOTER, OTHER) append type-specific columns."
                },
                "SLIDE_INFO_STRUC": {
                    "type": "object",
                    "description": "Fields to include in structural variant slides. Must contain a DEFAULT key."
                }
            }
        }
    },
    "allOf": [
        { "$ref": "#/definitions/input_output_options" },
        { "$ref": "#/definitions/miscellaneous" },
        { "$ref": "#/definitions/vep_options" },
        { "$ref": "#/definitions/vcfanno_options" },
        { "$ref": "#/definitions/short_variant_options" },
        { "$ref": "#/definitions/filtering_short" },
        { "$ref": "#/definitions/struc_variant_options" },
        { "$ref": "#/definitions/filtering_struc" },
        { "$ref": "#/definitions/reporting" }
    ]
}
